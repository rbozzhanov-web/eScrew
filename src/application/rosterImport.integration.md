Dependency: PR #45 (`core/aims-normalized-contract`).

The UI branch also merges current `main` so the AIMS waiting/importing/success/error/cancelled presentation from PR #44 is preserved while developing against the normalized Core contract.
